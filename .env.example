# Local and free by default. Requires the Ollama desktop app and a downloaded model.
LLM_BACKEND=ollama
OLLAMA_MODEL=qwen3:4b

# Optional paid backend. Leave blank when using Ollama locally.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini
